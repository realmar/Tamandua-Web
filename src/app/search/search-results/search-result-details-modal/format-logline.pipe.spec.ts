import { FormatLoglinePipe } from './format-logline.pipe';

describe('FormatLoglinePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatLoglinePipe();
    expect(pipe).toBeTruthy();
  });
});
